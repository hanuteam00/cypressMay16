/*
before(() => {
  cy.log(
    "Before all - This will run once before all tests"
  );
});

beforeEach(() => {
  cy.log(
    "Before each - This will run before each test"
  );
});

after(() => {
  cy.log(
    "After each - This will run after each test"
  );
});

after(() => {
  cy.log(
    "Before all - This will run once after all tests"
  );
});
*/